apiVersion: 1

providers:
  - name: DevOS
    orgId: 1
    folder: DevOS
    folderUid: devos
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
